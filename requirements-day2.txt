numpy
pandas
scikit-learn
torch
# Pinned: newer transformers (the "masking_utils" rewrite, ~2025 onward)
# dropped support for passing a fully custom (batch, seq, seq) attention
# mask directly to RobertaModel.forward() -- it now only accepts a plain
# 2D padding mask through that argument. GraphCodeBertEmbedder needs the
# old behaviour to inject the data-flow-graph attention pattern, so this
# version is pinned rather than left open.
transformers==4.46.3
tree-sitter
tree-sitter-python